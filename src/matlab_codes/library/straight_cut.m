function straight_cut(direction, length)
% library code for straight cut with selected direction and length in mm
    switch direction
        case 'forward'
            pass;
        case 'backward'
            pass;
        case 'left'
            pass;
        case 'right'
            pass;
        otherwise
            disp('direction not recognized');
    end
end